<nav class="bg-indigo-800 top-0 fixed z-50 w-full h-[60px] flex items-center justify-between px-5 sm:px-7 md:px-5 lg:px-6 xl:px-20 py-3">
	<div class="relative flex justify-between flex-wrap w-full min-w-full md:min-w-0 md:w-auto lg:static md:block lg:justify-start">
		<a data-turbo-cache="false" class="text-xl inline-block md:hidden lg:inline-block lg:text-xl xl:text-2xl py-2 empresa-nombre text-teal-50" href="{{path('inicio')}}">
			<span class="opacity-80" data-turbo-permanent {{ stimulus_controller({ typed: {strings: [ "En Guatemala desde 1958..." | trans , "abriendo caminos..." | trans , "acortando distancias..." | trans , "<span class='gold'>Transporte Fuente del Norte</span>" ]} }) }}></span>
			<span class="gold hidden" id="typed-placeholder">Transporte Fuente del Norte</span>
		</a>
		<button class="opacity-80 cursor-pointer text-xl leading-none px-3 py-1 border border-solid border-transparent rounded bg-transparent block md:hidden outline-none focus:outline-none" type="button" onclick="toggleNavbar('example-collapse-navbar')">
			<span class="material-symbols-outlined text-white text-3xl">
				menu
			</span>
		</button>
	</div>
	<div class="w-full md:w-auto px-5 md:px-0">
		<div class="bg-indigo-800 md:bg-transparent absolute top-[59px] w-full left-0 md:relative md:top-0 md:flex justify-end items-center lg:shadow-none hidden" id="example-collapse-navbar">
			<div>
				<span onclick="toggleNavbar('example-collapse-navbar')" class="material-symbols-outlined opacity-80 block md:hidden absolute mt-[10px] right-[9%] text-[25px] text-teal-50">
					close
				</span>
				<ul class="flex flex-col md:flex-row list-none mr-auto">
					<li class="flex items-center">
						<a class="menu-header" href="{{path('inicio')}}">
							{{'Comprar boleto' | trans}}</a>
					</li>
					<li class="flex items-center">
						<div class="relative group">
							<a class="menu-header" href="{{path('servicio')}}">
								{{'Servicios' | trans}}</a>
							<div class="absolute z-10 hidden md:group-hover:block bg-indigo-800 -mt-2 rounded py-3">
								<ul class="list-none">
									{% for key, servicio in this.servicios %}
										<li class="flex items-center">
											<a class="submenu-header" href="{{path('servicio', {'_fragment': servicio.slug})}}">
												{{servicio.nombre}}</a>
										</li>
									{% endfor %}
								</ul>
							</div>
						</div>
					</li>
					<li class="flex items-center">
						<a class="menu-header" href="{{path('estacion')}}">
							{{'Estaciones' | trans}}</a>
					</li>
					<li class="flex items-center">
						<a class="menu-header" href="{{path('historia')}}">
							{{'Quiénes somos' | trans}}</a>
					</li>
					<li class="flex items-center">
						<a class="menu-header mr-3" href="{{path('contacto')}}">
							{{'Contáctenos' | trans}}</a>
					</li>
				</ul>
			</div>
			<ul class="flex mt-3 mb-5 md:my-0">
				<li class="flex items-center mr-1">
					{{ component('button_link', {attr: 'data-turbo=false', text: 'English', link: '/en', class: 'sm font-semibold !px-2 !py-[3px] rounded'  ~ (app.request.locale == 'en' ? ' text-[13px] bg-teal-50 hover:bg-teal-50 text-slate-700' : ' text-[13px] btn-outline text-teal-50 border-cyan-400 hover:text-slate-900 hover:bg-teal-50 opacity-80')}) }}
				</li>
				<li class="flex items-center mr-1">
					{{ component('button_link', {attr: 'data-turbo=false', text: 'Español', link: '/', class: 'sm font-semibold !px-2 !py-[3px] rounded-md' ~ (app.request.locale == 'es' ? ' text-[13px] bg-teal-50 hover:bg-teal-50 text-slate-700' : ' text-[13px] btn-outline text-teal-50 border-cyan-400 hover:text-slate-900 hover:bg-teal-50 opacity-80')}) }}
				</li>
			</ul>
		</div>
	</div>

</nav>
<div class="h-[60px] w-full"></div>
