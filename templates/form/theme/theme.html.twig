{% use 'form/theme/tailwind_2_layout.html.twig' %}

{# {% block form_label %}
	{% set label_attr = {'class': 'block text-gray-700 text-sm font-bold mb-2'} %}
	{{ parent() }}
{% endblock %} #}

{% block form_widget_simple %}
	{% set attr = attr|merge({'class': 'input ' ~ (attr.class|default(''))|trim}) %}
	{{ parent() }}
{% endblock %}

{% block choice_widget %}
	{% set attr = attr|merge({'class': 'input ' ~ (attr.class|default(''))|trim}) %}
	{{ parent() }}
{% endblock %}

{# {% block form_row %}
	{% set row_attr = row_attr|merge({'class': 'mt-5 ' ~ (row_attr.class|default(''))|trim }) %}
	{{ parent() }}
{% endblock %} #}

{# {% block button_row %}
	{% set row_attr = row_attr|merge({'class': 'mt-5 ' ~ (row_attr.class|default(''))|trim }) %}
	{{ parent() }}
{% endblock button_row %} #}